/* General Styles */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #F5EFE7; /* Light background for content */
    color: #213555; /* Dark text color for high contrast */
    line-height: 1.6;
}

/* Header Styling (Category Title) */
header {
    background-color: #3E5879; /* Dark blue background for header */
    color: #F9F9F9; /* Off-white text color */
    text-align: center;
    padding: 2rem 0;
}

/* Project Category Section (Container) */
.project-category {
    padding: 3rem 2rem;
    background-color: #F9F9F9; /* Off-white background for the project container */
    margin-bottom: 3rem;
}

/* Project Item Layout (Image and Description side by side) */
.project-item {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 2rem;
}

/* Project Image Placeholder */
.project-image img {
    width: 100%;
    height: auto;
    max-width: 600px; /* You can adjust this for your layout */
    border-radius: 10px;
}

/* Project Description Styling */
.project-description {
    flex: 1;
    max-width: 600px;
}

.project-description h3 {
    font-size: 2rem;
    margin-bottom: 1rem;
    font-weight: bold;
}

.project-description p {
    font-size: 1rem;
    margin-bottom: 1.5rem;
    color: #333333; /* Dark text for description */
}

/* Video Section (Optional) */
.project-video {
    margin-top: 2rem;
    text-align: center;
}

.project-video iframe {
    width: 100%;
    max-width: 800px; /* Limit max width of the video */
    height: 450px; /* Standard video height */
    border-radius: 10px;
}

/* Section Divider (using background color) */
.divider {
    height: 2px;
    background-color: #D8C4B6; /* Beige color for divider */
    margin-top: 3rem;
}

/* Responsive Design - Make sure the layout is mobile-friendly */
@media screen and (max-width: 768px) {
    .project-item {
        flex-direction: column;
        align-items: center;
    }

    .project-image, .project-description {
        max-width: 100%;
    }

    .project-video iframe {
        width: 100%;
    }
}
